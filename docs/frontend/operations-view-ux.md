# Operations View UX

**Status:** Accepted  

Director Operations is a read-only attention board over existing modules. Each KPI and attention row deep-links to the object list or detail. Severity is never color-only (label + icon/text).
