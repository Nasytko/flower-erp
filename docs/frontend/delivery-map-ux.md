# Delivery Map UX

Provider-neutral map contract. Desktop: map + synchronized list. Mobile: map/list toggle + bottom sheet. Points without coordinates listed as «Требуют уточнения адреса». balanceDue only with `delivery:view-payment-summary`. No Google Maps by default — placeholder/list mode until licensed provider.
