import { Module } from '@nestjs/common';

/**
 * Empty module container for bounded context `notifications`.
 * No controllers, services, entities, or repositories in the scaffold phase.
 */
@Module({})
export class NotificationsModule {}
